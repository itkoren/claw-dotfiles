export OP_SERVICE_ACCOUNT_TOKEN={{ .data.opsakey | quote }}
# Verify authentication works
op account list >/dev/null 2>&1 || {
  echo "Authenticating with 1Password..."
  op account add --address my.1password.com --signin
}
